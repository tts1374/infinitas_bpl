interface:
  display_name: "FSM Protocol Guard"
  short_description: "Guard FSM and WS contract changes"
  default_prompt: "Detect risky FSM/protocol changes and produce required invariants and validation checklist before implementation."
