import { SOURCE_TYPES } from "../enums/source";

export const SOURCE_OPTIONS = SOURCE_TYPES;

export const SOURCE_FIXED_PER_DEVICE = true;
export const SOURCE_CHANGE_ALLOWED_IN_ROOM = false;

export const SOURCE_ERROR_CODE = "SOURCE_UNAVAILABLE" as const;
